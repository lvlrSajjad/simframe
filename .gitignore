node_modules/
*.log
.DS_Store
simframe.png
simframe-strip.png

# Swift build output — regenerated by `swift build`, never committed
native/simframed/.build/
native/simframed/.swiftpm/

# Strings that must not enter this repo — see scripts/check-private.mjs
.private-strings
